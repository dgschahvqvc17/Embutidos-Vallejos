@model RegisterViewModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crear Cuenta - Embutidos Vallejos</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        :root { --primary: #7A1F2A; --accent: #C9A94E; --bg-body: #0B0C10; --bg-card: #15161C; --bg-input: #1E1F28; --border-color: #262838; --text-primary: #E8E8EE; --text-secondary: #fff; --text-muted: #55566A; --danger: #FF1744; --font-display: 'Playfair Display', serif; --font-body: 'Inter', sans-serif; }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: var(--font-body); min-height: 100vh; display: flex; align-items: center; justify-content: center; position: relative; overflow-x: hidden; padding: 40px 0; }
        .video-bg { position: fixed; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; z-index: -2; }
        
        /* Modificado: Contraste mejorado del overlay */
        .video-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(135deg, rgba(10,11,15,0.85) 0%, rgba(0,0,0,0.65) 50%, rgba(10,11,15,0.8) 100%); z-index: -1; }
        
        .back-home { position: fixed; top: 24px; left: 24px; z-index: 10; }
        
        /* Modificado: Textos mas blancos */
        .back-home a { display: inline-flex; align-items: center; gap: 6px; color: rgba(255,255,255,0.9); text-decoration: none; font-size: 13px; font-weight: 500; padding: 8px 16px; border-radius: 6px; background: rgba(21,22,28,0.6); border: 1px solid rgba(38,40,56,0.5); backdrop-filter: blur(10px); transition: all 0.25s ease; }
        .back-home a:hover { color: var(--accent); border-color: var(--accent); transform: translateX(-3px); }
        .register-container { width: 100%; max-width: 500px; padding: 20px; position: relative; z-index: 1; }
        .register-card { padding: 38px 36px 32px; border-radius: var(--radius-lg); }
        .logo-section { text-align: center; margin-bottom: 28px; }
        .logo-title { font-family: var(--font-display); font-weight: 900; font-size: 26px; color: var(--text-primary); letter-spacing: -0.3px; text-shadow: 0 2px 10px rgba(0,0,0,0.5); }
        .logo-title span { color: var(--accent); }
        
        /* Modificado: Subtitulo blanco */
        .logo-subtitle { color: #ffffff; font-size: 11px; letter-spacing: 3px; text-transform: uppercase; margin-top: 6px; font-weight: 600; text-shadow: 0 1px 3px rgba(0,0,0,0.5); }
        
        /* Modificado: Labels mas blancos */
        .form-label { color: rgba(255,255,255,0.9); font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 6px; display: inline-block; }
        .form-control { padding: 12px 14px; padding-left: 44px; font-size: 15px; height: 48px; border-radius: var(--radius-sm); color: #ffffff; }
        
        /* Modificado: Placeholders blancos sutiles */
        .form-control::placeholder { color: rgba(255, 255, 255, 0.7) !important; }
        
        .form-control:-webkit-autofill, .form-control:-webkit-autofill:hover, .form-control:-webkit-autofill:focus { -webkit-text-fill-color: #ffffff !important; -webkit-box-shadow: 0 0 0px 1000px rgba(42,44,58,0.9) inset !important; transition: background-color 5000s ease-in-out 0s; caret-color: #ffffff; }
        
        .password-input { padding-right: 48px !important; }

        .input-group { position: relative; }
        
        /* Modificado: Iconos mas blancos */
        .input-icon { position: absolute; left: 16px; top: 50%; transform: translateY(-50%); color: rgba(255,255,255,0.8); font-size: 15px; z-index: 4; pointer-events: none; transition: color 0.3s; }
        .form-control:focus ~ .input-icon, .form-control:focus + .input-icon { color: var(--accent); }
        
        /* Modificado: Nuevo boton de ojito tipo burbuja */
        .toggle-password { 
            position: absolute; 
            right: 8px; /* Ajuste para el circulo */
            top: 50%; 
            transform: translateY(-50%); 
            color: rgba(255,255,255,0.9); 
            background: rgba(11, 12, 16, 0.6); /* Fondo oscuro semitransparente */
            border: 1px solid rgba(255,255,255,0.15); /* Borde super sutil */
            border-radius: 50%; /* Círculo perfecto */
            cursor: pointer; 
            font-size: 14px; 
            padding: 0;
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 4; 
            transition: all 0.2s; 
        }
        .toggle-password:hover { 
            color: var(--accent); 
            background: rgba(0,0,0,0.8);
            border-color: rgba(201, 169, 78, 0.4);
        }
        
        .btn-register { width: 100%; margin-top: 8px; font-size: 15px; padding: 14px; }
        .text-validation { color: var(--danger); font-size: 11px; margin-top: 2px; display: block; }
        .alert-danger { background: rgba(255,23,68,0.08); border: 1px solid rgba(255,23,68,0.15); border-radius: 6px; color: var(--danger); font-size: 12px; padding: 10px 14px; margin-bottom: 16px; }
        .alert-danger ul { list-style: none; padding: 0; margin: 0; }
        
        .login-link { text-align: center; margin-top: 16px; }
        /* Modificado: Texto mas blanco */
        .login-link span { color: rgba(255,255,255,0.85); font-size: 13px; }
        .login-link a { color: var(--accent); text-decoration: none; font-weight: 600; transition: color 0.2s; }
        .login-link a:hover { color: var(--accent); text-decoration: underline; }
        
        /* Modificado: Footer blanco */
        .footer-text { text-align: center; margin-top: 16px; color: rgba(255,255,255,0.7); font-size: 10px; }
        .row-fields { margin-bottom: 14px; }
        
        .phone-wrapper { position: relative; }
        /* Modificado: Prefijo +591 mas visible */
        .phone-prefix { position: absolute; left: 40px; top: 50%; transform: translateY(-50%); color: rgba(255,255,255,0.8); font-weight: 600; font-size: 13px; z-index: 4; pointer-events: none; letter-spacing: 0.3px; }
        .phone-input { padding-left: 76px !important; }
        
        @@media (max-width: 480px) { .register-card { padding: 26px 18px 22px; } .back-home { top: 16px; left: 16px; } .back-home a { font-size: 11px; padding: 6px 12px; } }
    </style>
</head>
<body>
    <video class="video-bg" autoplay muted loop playsinline>
        <source src="~/video/video1.mp4" type="video/mp4" />
    </video>
    <div class="video-overlay"></div>

    <div class="back-home">
        <a asp-controller="Home" asp-action="Index"><i class="bi bi-arrow-left"></i> Volver al inicio</a>
    </div>
    <div class="register-container animate-fade-up">
        <div class="register-card glass-panel glow-border">
            <div class="logo-section">
                <div class="logo-title">Crear <span class="glow-text">Cuenta</span></div>
                <div class="logo-subtitle">Embutidos Vallejos</div>
            </div>

            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert-danger"><div asp-validation-summary="ModelOnly"></div></div>
            }

            <form asp-action="Register" method="post">
                <div class="row g-3">
                    <div class="col-md-6 row-fields">
                        <label asp-for="Nombre" class="form-label"><i class="bi bi-person me-1"></i> @Html.DisplayNameFor(m => m.Nombre)</label>
                        <div class="input-group">
                            <input asp-for="Nombre" class="form-control glass-input" placeholder="Tu nombre" />
                            <i class="bi bi-person input-icon"></i>
                        </div>
                        <span asp-validation-for="Nombre" class="text-validation"></span>
                    </div>
                    <div class="col-md-6 row-fields">
                        <label asp-for="Apellido" class="form-label"><i class="bi bi-person me-1"></i> @Html.DisplayNameFor(m => m.Apellido)</label>
                        <div class="input-group">
                            <input asp-for="Apellido" class="form-control glass-input" placeholder="Tu apellido" />
                            <i class="bi bi-person input-icon"></i>
                        </div>
                        <span asp-validation-for="Apellido" class="text-validation"></span>
                    </div>
                </div>

                <div class="row-fields">
                    <label asp-for="Telefono" class="form-label"><i class="bi bi-telephone me-1"></i> @Html.DisplayNameFor(m => m.Telefono)</label>
                    <div class="input-group phone-wrapper">
                        <span class="phone-prefix">+591</span>
                        <input asp-for="Telefono" class="form-control glass-input phone-input" id="telefono" placeholder="70000000" maxlength="12" value="+591" />
                        <i class="bi bi-telephone input-icon"></i>
                    </div>
                    <span asp-validation-for="Telefono" class="text-validation"></span>
                    <small style="color: rgba(255,255,255,0.7); font-size: 10px; margin-top: 4px; display: block;"><i class="bi bi-info-circle me-1"></i> Ingresa 8 digitos despues de +591</small>
                </div>

                <div class="row-fields">
                    <label asp-for="Email" class="form-label"><i class="bi bi-envelope me-1"></i> @Html.DisplayNameFor(m => m.Email)</label>
                    <div class="input-group">
                        <input asp-for="Email" class="form-control glass-input" placeholder="tu@correo.com" />
                        <i class="bi bi-envelope input-icon"></i>
                    </div>
                    <span asp-validation-for="Email" class="text-validation"></span>
                </div>

                <div class="row-fields">
                    <label asp-for="Direccion" class="form-label"><i class="bi bi-geo-alt me-1"></i> @Html.DisplayNameFor(m => m.Direccion)</label>
                    <div class="input-group">
                        <input asp-for="Direccion" class="form-control glass-input" placeholder="Calle, ciudad, zona" />
                        <i class="bi bi-geo-alt input-icon"></i>
                    </div>
                    <span asp-validation-for="Direccion" class="text-validation"></span>
                </div>

                <div class="row g-3">
                    <div class="col-md-6 row-fields">
                        <label asp-for="Password" class="form-label"><i class="bi bi-lock me-1"></i> @Html.DisplayNameFor(m => m.Password)</label>
                        <div class="input-group">
                            <input asp-for="Password" class="form-control glass-input password-input" id="password" placeholder="Min 6 caracteres" />
                            <i class="bi bi-lock input-icon"></i>
                            <button type="button" class="toggle-password" onclick="togglePwd('password','toggleIcon1')"><i class="bi bi-eye" id="toggleIcon1"></i></button>
                        </div>
                        <span asp-validation-for="Password" class="text-validation"></span>
                    </div>
                    <div class="col-md-6 row-fields">
                        <label asp-for="ConfirmPassword" class="form-label"><i class="bi bi-lock me-1"></i> @Html.DisplayNameFor(m => m.ConfirmPassword)</label>
                        <div class="input-group">
                            <input asp-for="ConfirmPassword" class="form-control glass-input password-input" id="confirmPassword" placeholder="Confirmar" />
                            <i class="bi bi-lock input-icon"></i>
                            <button type="button" class="toggle-password" onclick="togglePwd('confirmPassword','toggleIcon2')"><i class="bi bi-eye" id="toggleIcon2"></i></button>
                        </div>
                        <span asp-validation-for="ConfirmPassword" class="text-validation"></span>
                    </div>
                </div>

                <button type="submit" class="btn-premium btn-register mt-2"><i class="bi bi-person-plus me-2"></i> Crear Cuenta</button>
            </form>

            <div class="login-link">
                <span>¿Ya tienes cuenta?</span>
                <a asp-controller="Account" asp-action="Login">Inicia Sesion <i class="bi bi-arrow-right ms-1" style="font-size: 11px;"></i></a>
            </div>

            <div class="footer-text">&copy; 2026 Embutidos Vallejos</div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
    <script>
        function togglePwd(inputId, iconId) {
            const input = document.getElementById('password');
            const icon = document.getElementById(iconId);
            if (input.type === 'password') { input.type = 'text'; icon.classList.replace('bi-eye', 'bi-eye-slash'); }
            else { input.type = 'password'; icon.classList.replace('bi-eye-slash', 'bi-eye'); }
        }
        document.addEventListener('DOMContentLoaded', function() {
            const telInput = document.getElementById('telefono');
            if (telInput) {
                telInput.addEventListener('input', function(e) {
                    let val = this.value;
                    if (!val.startsWith('+591')) { val = '+591' + val.replace(/[^0-9]/g, ''); }
                    let digits = val.replace('+591', '').replace(/\D/g, '').slice(0, 8);
                    this.value = '+591' + digits;
                });
            }
        });
    </script>
</body>
</html>
